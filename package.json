{
  "name": "project-root",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "concurrently --kill-others --names server,web \"bun --cwd apps/server dev\" \"bun --cwd apps/web dev\"",
    "build": "bun --filter \"*\" build",
    "typecheck": "bun --filter \"*\" typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
